# SonarCloud AutoScan configuration.
# Enable AutoScan on the project at sonarcloud.io (org: gundestrup);
# the project key binding lives there, not in this file.
# Keep exclusion boundaries in sync with codecov.yml ignore: and
# .codefactor.yml exclude:.
sonar.sources=lib
sonar.tests=spec
sonar.exclusions=docs/**
sonar.test.exclusions=spec/**
